// 
/**
 * # Conversão de tipos
 * de número para string
 * variável.toString()
 * 
 * string para número
 * parseInt(variável)
 * 
 *  # OPERÁDORES ARITIMÉTICOS
 * + soma
 * - subtrai
 * * multiplica
 * / divide
 * % resto da divisão
 * ** potencia
 * 
 * # OPERÁDORES RELACIONAIS
 * > MAIOR
 * < MENOR
 * >=MAIOR OU IGUAL
 * <= MENOR OU IGUAL
 * == IGUAL
 * === SE OS TIPOS SÃO IGUAIS
 * != DIFERENTE
 * 
 * # OPERADORES LÓGICOS
 * '!  Não - negação
 * '&& E - conjunção
 * '|| OU - disjunção
 * 
 * # VARIÁVEIS
 * var - vc pode redeclarar - escopo global no código
 * let - não pode redeclarar - escopo local no código
 * const - constantes, algo que vc espera que não mude
 * "nada"
 * 
 * # HOISTING = elevar/subir
 * lê duas vezes o cód
 * Primeiro ele entende o que é funções e variáveis e depois
 * le novamente e ai executa o seu codigo
 * menor()
 * def menor():
 *  return n1
 * 
 * # Array = listas
 * # Objetos = dicionarios
 * json => javaScriot object notation {chave: valor}
 * 
 * 
 * saber o tipo de uma variável
 * typeof()
 * 
 * 
 * # funções
 * function nome_da_função(parametros)
 * {
 *      return alguma_coisa
 * }
 * 
 * # codicionais
 * if (condition1) {
  //  block of code to be executed if condition1 is true
    } else if (condition2) {
    //  block of code to be executed if the condition1 is false and condition2 is true
    } else {
    //  block of code to be executed if the condition1 is false and condition2 is false
    }

 * # loops
    for (let i = 0; i < cars.length; i++) {
    text += cars[i] + "<br>";
    }
 */